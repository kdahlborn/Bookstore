import Cart from './Cart';
import Logo from './Logo';

const Header = () => {
    return (
        <header className="header">
            <Logo />
            <Cart />
        </header>
    );
};

export default Header;
